{
  "name": "stream-sync",
  "private": true,
  "version": "2.1.0",
  "description": "Stream Sync 2.1 — Rust/Tauri desktop and overlay server",
  "scripts": {
    "dev": "node scripts/dev.js",
    "dev:watch": "node scripts/dev.js --watch",
    "prepare-release": "node scripts/prepare-release.js",
    "build": "npm run prepare-release && node scripts/build.js",
    "test:js": "node scripts/check-privileged-fetch.js && node --test tests/events-variation-picker.test.js tests/events-alert-delivery.test.js tests/kick-connect-recursion.test.js tests/events-studio-platform.test.js tests/validate-release-tag.test.js tests/generate-latest-json.test.js tests/update-modal.test.js",
    "test:version": "node scripts/check-version-consistency.js",
    "test:icons": "node scripts/check-desktop-icons.js",
    "test": "npm run test:version && npm run test:icons && npm run test:js && cargo test --workspace"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
